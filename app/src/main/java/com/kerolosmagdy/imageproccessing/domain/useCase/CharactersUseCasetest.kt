package com.kerolosmagdy.imageproccessing.domain.useCase

import javax.inject.Inject

class CharactersUseCasetest @Inject constructor(
//    private val repository: MarvelRepository
) {

//    operator fun invoke(offset: Int): Flow<Resource<List<Character>>> = flow {
//        try { emit(Resource.Loading())
//            val list = repository.getAllCharacter(offset = offset).data.results.map {
//                it.toCharacter()
//            }
//            emit(Resource.Success(list))
//
//        } catch (e: Exception) {
//            emit(Resource.Error(e.printStackTrace().toString()))
//        }
//    }
}